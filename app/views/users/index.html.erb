<div class="container mt-4">
  <div class="row">
      <%= render 'list', book: @book, user: @user %>
    <div class="col-8">
      <h2>Users</h2>
      <table class="table table-hover" >
        <thead>
          <tr>
            <th></th>
            <th class="image">Image</th>
            <th class="name">Name</th>
          </tr>
        </thead>
        <tbody>
        <% @users.each do |user| %>
          <tr>
            <td><%= image_tag user.get_profile_image(30,30) %>
            </td>
            <td><div class"name"><%= user.name %></p>
            </td>
            <td>
              <%= link_to user_path(user.id) do %>
                <div>Show</div>
              <% end %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
      <%= paginate @users %>
    </div>
  </div>
</div>