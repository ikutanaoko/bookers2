<div class="container">
  <div class="row">
    <div class="mx-auto mt-4 ">
      <h1>Edittig Book</h1>
      <% if @book.errors.any? %>
　       <%= @book.errors.count %>　errors prohibited this book from being saved:
          <ul>
          <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
      <% end %>
      <%= form_with model: @book do |f| %>
        <div class="form-group">
          <lavel for-"title">Title</lavel>
          <div><%= f.text_field :title, autofocus: true, id:"inputTitle", size: 55 %></div>
        </div>
        <div class="form-group">
          <lavel for-"body">Opinion</lavel>
          <div><%= f.text_area :body, id:"inputTitle", size: "60×3" %></div>
        </div>
        <div class="form-group">
          <%= f.submit 'Update Book',class:"btn btn-success" %></div>
      <% end %>
      <%= link_to "Show", book_path %> | <%= link_to "Back", books_path %>
    </div>
  </div>
</div>